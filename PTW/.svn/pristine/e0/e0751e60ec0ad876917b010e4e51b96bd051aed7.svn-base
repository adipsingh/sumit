using MODS.PTW.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace MODS.PTW.Services
{
    public interface IUserAuthorityService
    {
        UserAuthority Create(UserAuthority userAuthority );
        List<UserAuthority> GetAllUserAuthority();
        UserAuthority GetById(int id);
        UserAuthority Update(UserAuthority userAuthority, int id);
        UserAuthority Delete(int id);
    }
}
